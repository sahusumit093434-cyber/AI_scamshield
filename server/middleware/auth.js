import jwt from 'jsonwebtoken'; import mongoose from 'mongoose'; import User from '../models/User.js'; import { inMemoryUsers } from '../mockDb.js'; export const protect=async(req,res,next)=>{try{const token=req.headers.authorization?.split(' ')[1];if(!token)throw Error('Authentication required');const d=jwt.verify(token,process.env.JWT_SECRET);if(mongoose.connection.readyState===1){req.user=await User.findById(d.id);}else{req.user=inMemoryUsers.find(u=>u._id.toString()===d.id.toString());}if(!req.user)throw Error('User no longer exists');next()}catch(e){e.status=401;next(e)}}; export const admin=(req,res,next)=>req.user?.role==='admin'?next():res.status(403).json({message:'Admin access required'});
