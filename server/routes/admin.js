import {Router} from 'express'; import User from '../models/User.js'; import Analysis from '../models/Analysis.js'; import Report from '../models/Report.js'; import {protect,admin} from '../middleware/auth.js'; const r=Router();r.use(protect,admin);r.get('/stats',async(req,res,next)=>{try{const [users,analyses,reports,risks]=await Promise.all([User.countDocuments(),Analysis.countDocuments(),Report.countDocuments(),Analysis.aggregate([{$group:{_id:'$riskLevel',count:{$sum:1}}}])]);res.json({users,analyses,reports,risks})}catch(e){next(e)}});r.get('/users',async(req,res,next)=>{try{res.json(await User.find().select('-password').sort('-createdAt').limit(100))}catch(e){next(e)}});r.delete('/users/:id',async(req,res,next)=>{try{if(req.params.id===String(req.user._id))return res.status(400).json({message:'Cannot delete yourself'});await User.findByIdAndDelete(req.params.id);res.status(204).end()}catch(e){next(e)}});export default r;
